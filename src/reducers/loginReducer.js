//initialize state
const initialState = {
    userId: {}
}

//register reducer
function loginReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}

//export reducer
export default loginReducer