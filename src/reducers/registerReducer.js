//initialize state
const initialState = {
    newUser: {}
}

//register reducer
function registerReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}

//export reducer
export default registerReducer